% ==========================================================================
% numbers_leak_trainseed.tex — GENERATED by
% facebench/eval/emit_numbers_leak_trainseed.py
% Do not edit by hand; re-run the emitter.
%
% The leakage contrast with BOTH arms as five-retraining means.
% Leaked arm:  B{1,2}_scanlevel_ts{1..5}, trained on the frozen leaky
%              manifests under data/processed/legacy_scanlevel/ and scored
%              by rerun_scanlevel.py --train-seed at five inference seeds,
%              CPU/16 threads, split seed frozen at 42.
% Correct arm: B{1,2}_corrected_ts{1..5} from the 2026-08-31 sweep.
% Inference seeds are collapsed WITHIN each retraining before the SD is
% taken, so FPS noise does not inflate it.
%
% SUPERSEDES \leakSyn*/\leakDx*/\leakDxFone* in numbers_leak.tex.
% DOES NOT supersede \leakComb* — C_scanlevel was never retrained.
%
% NOT PAIRED. Different test sets by construction; no interval may be
% attached to the difference. Use the Sep macros for a pairing-free claim.
% ==========================================================================

% scanlevel_trainseed_summary.csv | 19-class syndrome, accuracy
%   leaked  B1_scanlevel: n_train_seeds=5 n_units=1938 n_subjects=1602 leaked_scan_frac=0.787
%   correct: mean=0.2668 sd=0.0288
\newcommand{\tsLeakSynBefore}{0.338}
\newcommand{\tsLeakSynBeforeSD}{0.040}
\newcommand{\tsLeakSynBeforeRange}{[0.278, 0.372]}
\newcommand{\tsLeakSynAfter}{0.267}
\newcommand{\tsLeakSynAfterSD}{0.029}
\newcommand{\tsLeakSynAfterRange}{[0.222, 0.297]}
\newcommand{\tsLeakSynDelta}{7}  % pp, difference of means, NOT paired
\newcommand{\tsLeakSynDeltaOne}{7.2}
\newcommand{\tsLeakSynSep}{no}  % worst leaked (0.278) vs best subject-disjoint (0.297)
\newcommand{\tsLeakSynLegZ}{-0.01}  % single unseeded leaky checkpoint = 0.338, in SD units of the retrained leaked arm

% scanlevel_trainseed_summary.csv | 33-class clinical, accuracy
%   leaked  B2_scanlevel: n_train_seeds=5 n_units=530 n_subjects=427 leaked_scan_frac=0.853
%   correct: mean=0.2951 sd=0.0294
\newcommand{\tsLeakDxBefore}{0.523}
\newcommand{\tsLeakDxBeforeSD}{0.034}
\newcommand{\tsLeakDxBeforeRange}{[0.465, 0.550]}
\newcommand{\tsLeakDxAfter}{0.295}
\newcommand{\tsLeakDxAfterSD}{0.029}
\newcommand{\tsLeakDxAfterRange}{[0.263, 0.339]}
\newcommand{\tsLeakDxDelta}{23}  % pp, difference of means, NOT paired
\newcommand{\tsLeakDxDeltaOne}{22.8}
\newcommand{\tsLeakDxSep}{yes}  % worst leaked (0.465) vs best subject-disjoint (0.339)
\newcommand{\tsLeakDxLegZ}{-0.33}  % single unseeded leaky checkpoint = 0.512, in SD units of the retrained leaked arm

% scanlevel_trainseed_summary.csv | 33-class clinical, macro-F1
%   leaked  B2_scanlevel: n_train_seeds=5 n_units=530 n_subjects=427 leaked_scan_frac=0.853
%   correct: mean=0.2476 sd=0.0266
\newcommand{\tsLeakDxFoneBefore}{0.479}
\newcommand{\tsLeakDxFoneBeforeSD}{0.030}
\newcommand{\tsLeakDxFoneBeforeRange}{[0.429, 0.506]}
\newcommand{\tsLeakDxFoneAfter}{0.248}
\newcommand{\tsLeakDxFoneAfterSD}{0.027}
\newcommand{\tsLeakDxFoneAfterRange}{[0.220, 0.285]}
\newcommand{\tsLeakDxFoneDelta}{23}  % pp, difference of means, NOT paired
\newcommand{\tsLeakDxFoneDeltaOne}{23.1}
\newcommand{\tsLeakDxFoneSep}{yes}  % worst leaked (0.429) vs best subject-disjoint (0.285)
\newcommand{\tsLeakDxFoneLegZ}{-0.17}  % single unseeded leaky checkpoint = 0.474, in SD units of the retrained leaked arm

% Leak fraction — unchanged by retraining (it is a property of the
% split, not of the model), but re-emitted here from the retrained
% arm's own CSV so this file does not depend on numbers_leak.tex.
\newcommand{\tsLeakSynLeak}{79\%}
\newcommand{\tsLeakDxLeak}{85\%}
\newcommand{\tsLeakDxFoneLeak}{85\%}

