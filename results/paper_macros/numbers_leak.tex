% ==========================================================================
% numbers_leak.tex — GENERATED by scripts/paper_reruns/emit_numbers_scanlevel.py
% Do not edit by hand; re-run the emitter.
%
% The scan-level ('before') arm of the leakage audit, re-scored under the
% SAME protocol as the subject-disjoint arm: original checkpoints, legacy
% scan-level split rebuilt from dataset.py at commit 1d4ec45, five inference
% seeds, 2000-resample bootstrap over test SUBJECTS, CPU at 16 threads.
%
% SUPERSEDES the \prov single-run values in numbers.tex (\leakDxBefore etc.).
%
% NOT PAIRED. The two arms have different test sets, so the difference
% between them carries NO confidence interval. Quote each arm with its own
% interval and the delta as a difference of point estimates.
% ==========================================================================

% scanlevel_summary_ci_all.csv | task=B1_scanlevel metric=accuracy n_seeds=5 n_units=1938 n_subjects=1602
\newcommand{\leakSynBeforeS}{0.338}
\newcommand{\leakSynBeforeSD}{0.0038}  % inference-seed SD
\newcommand{\leakSynBeforeCI}{[0.318, 0.357]}  % 95% bootstrap over test subjects
\newcommand{\leakSynDeltaS}{4}  % pp, 0.338 -> 0.295; difference of point estimates, NOT paired

% scanlevel_summary_ci_all.csv | task=B2_scanlevel metric=accuracy n_seeds=5 n_units=530 n_subjects=427
\newcommand{\leakDxBeforeS}{0.512}
\newcommand{\leakDxBeforeSD}{0.0070}  % inference-seed SD
\newcommand{\leakDxBeforeCI}{[0.471, 0.552]}  % 95% bootstrap over test subjects
\newcommand{\leakDxDeltaS}{22}  % pp, 0.512 -> 0.291; difference of point estimates, NOT paired

% scanlevel_summary_ci_all.csv | task=B2_scanlevel metric=macro_f1 n_seeds=5 n_units=530 n_subjects=427
\newcommand{\leakDxFoneBeforeS}{0.474}
\newcommand{\leakDxFoneBeforeSD}{0.0116}  % inference-seed SD
\newcommand{\leakDxFoneBeforeCI}{[0.419, 0.505]}  % 95% bootstrap over test subjects
\newcommand{\leakDxFoneDeltaS}{23}  % pp, 0.474 -> 0.240; difference of point estimates, NOT paired

% scanlevel_summary_ci_all.csv | task=C_scanlevel metric=accuracy n_seeds=5 n_units=3690 n_subjects=3411
\newcommand{\leakCombBeforeS}{0.906}
\newcommand{\leakCombBeforeSD}{0.0018}  % inference-seed SD
\newcommand{\leakCombBeforeCI}{[0.897, 0.915]}  % 95% bootstrap over test subjects
\newcommand{\leakCombDeltaS}{0}  % pp, 0.906 -> 0.903; difference of point estimates, NOT paired

% Fraction of scans in the legacy TEST set whose subject also appears in
% the legacy TRAIN set. This is the leak itself, and it is what makes the
% inflation task-dependent: the binary task leaks far less than the
% fine-grained ones. Source: leaked_scan_frac in the same CSV.
\newcommand{\leakSynLeakFrac}{79\%}
\newcommand{\leakDxLeakFrac}{85\%}
\newcommand{\leakDxFoneLeakFrac}{85\%}
\newcommand{\leakCombLeakFrac}{36\%}

