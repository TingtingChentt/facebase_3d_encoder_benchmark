% ==========================================================================
% RECOGNIZABILITY — generated by recognizability.py. DO NOT HAND-EDIT.
% Resolves the poster-vs-results_analysis rho conflict: the two documents
% computed DIFFERENT TASKS. Both values below are from the seeded 5-seed
% predictions. Intervals bootstrap over CLASSES (seed 0, 2000 draws).
% arm: trainseed  — five independently trained models, inference seeds reduced within each run first
% ==========================================================================

% 33-class clinical diagnosis, subject level, 33 classes
\newcommand{\rhoSizeDx}{+0.37}
\newcommand{\rhoSizeDxCI}{[-0.00, +0.66]}
\newcommand{\varExplDx}{17\%}

% 19-class syndrome category, subject level, 19 classes
\newcommand{\rhoSizeSyn}{-0.31}
\newcommand{\rhoSizeSynCI}{[-0.81, +0.24]}

% classes with 5-12 test subjects: 13
\newcommand{\fOneBandLo}{5}
\newcommand{\fOneBandHi}{12}
\newcommand{\fOneBandN}{13}
\newcommand{\fOneSpanLo}{0.03}
\newcommand{\fOneSpanHi}{0.66}

% RASopathy confusions — ENRICHMENT, not dominance. The draft's
% "confused with one another more than with anything else" is FALSE:
% the plurality of their errors leave the group, top destination Joubert Syndrome.
\newcommand{\rasWithinFrac}{9\%}
\newcommand{\rasShare}{9\%}
\newcommand{\rasEnrich}{1}
% per-run within-fraction range over 5 training run(s): 2% to 18%
\newcommand{\rasWithinFracLo}{2\%}
\newcommand{\rasWithinFracHi}{18\%}

% These four were hand-maintained in numbers_sal_err.tex until 2026-09-08 and
% described the single pre-sweep checkpoint. Emitted here so they cannot drift
% from \rasWithinFrac / \rasEnrich again.
% top destination counted over 5 training run(s)
\newcommand{\rasNErr}{236}
\newcommand{\rasOutFrac}{91\%}
\newcommand{\rasTopDest}{Joubert syndrome}
\newcommand{\rasTopDestFrac}{21\%}

